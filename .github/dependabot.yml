version: 2
updates:
    # Keeps the vendor/web-links submodule pin (the scraper contract) moving
    # with upstream via automatic PRs.
    - package-ecosystem: gitsubmodule
      directory: /
      schedule:
          interval: daily
